  gameroom_rgb_colorchanger:
    alias: Gameroom Colorchanger
    sequence:
      - service: logbook.log
        data_template:
          name: "Color select"
          message: "Selected the {{ states.input_select.led_light_colors.state | string }} color"
      - service: light.turn_on
        data_template:
          entity_id: light.gameroom_rgb_light
          color_name: >-
            {% if is_state("input_select.led_light_colors", "red") %}
              red
            {% else if is_state("input_select.led_light_colors", "blue") %}
              blue
            {% else if is_state("input_select.led_light_colors", "yellow") %}
              yellow
            {% else if is_state("input_select.led_light_colors", "green") %}
              green
            {% else if is_state("input_select.led_light_colors", "purple") %}
              purple
            {% else if is_state("input_select.led_light_colors", "white") %}
              white
            {% endif %}
      - service: input_select.select_next
        data:
          entity_id: input_select.led_light_colors
