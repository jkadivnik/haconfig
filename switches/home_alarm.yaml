  platform: template
  switches:
    alarm_toggle:
      friendly_name: Alarm Toggle
      value_template: "{{ is_state(input_boolean.alarm_armed, 'on') }}"
      turn_on:
        service: input_boolean.turn_on
        data:
          entity_id: input_boolean.alarm_armed
      turn_off:
        service: input_boolean.turn_off
        data:
          entity_id: input_boolean.alarm_armed
