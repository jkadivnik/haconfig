  platform: template
  sensors:
    gameroom_harmony_activity:
      friendly_name: "Gameroom Harmony Status"
      value_template: >-
        {% if is_state("remote.gameroom_hub", 'on') %}
          {{ states.remote.gameroom_hub.attributes.current_activity }}
        {% else %}
          PowerOff
        {% endif %}
