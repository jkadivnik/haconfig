  platform: template
  sensors:
    gameroom_harmony_activity:
      friendly_name: "Gameroom Harmony Status"
      value_template: >-
        {% if is_state("remote.game_room_hub", 'on') %}
          {{ states.remote.game_room_hub.attributes.current_activity }}
        {% else %}
          PowerOff
        {% endif %}
